export const goToPrecos = (navigate) => {
    navigate(`/precos`)
}

export const goToPlanos = (navigate) => {
    navigate(`/planos`)
}

export const goToBeneficiaries = (navigate) => {
    navigate(`/beneficiarios`)
}

export const goToCreateBeneficiary = (navigate) => {
    navigate(`/criarBeneficiario`)
}

export const goToProposals = (navigate) => {
    navigate(`/propostas`)
}

export const goBack = (navigate) => {
    navigate('/')
}